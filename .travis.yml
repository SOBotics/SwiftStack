language: generic
sudo: required
env: SWIFT_VERSION=5.2

matrix:
  include:
    - os: linux
      dist: trusty
    - os: osx
      osx_image: xcode11

install: eval "$(curl -sL https://swiftenv.fuller.li/install.sh)" 

script:
  - swift test
